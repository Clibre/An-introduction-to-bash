## Exercise 2 ##
Print every lines that doesn't end with a period or a whitespace character. in  
the file input.txt to a file named output.txt (use the redirection ">")
 
## Protip: ##
Use the -v option so only the lines that doesn't match the pattern
will be printed.
Only space and tabulation will be considered as whitespace characters.
In your regex, use \t to designate a tabulation
