bash_profile vs bashrc

bashrc:
	PS1
	alias
	dircolor / LS_COLORS ???

variable d'environnement

commande bash:
	simple:
		ls  => 1, l, a/A, R, h(human readable)
		cd => relative vs absolute path
		pwd => print working directory
		rm
		cp
		mv
		mkdir
		rmdir
		sudo
		man

		passwd => modify password
		history
		jobs
		fg
		cat
		more / less
		tail / head
		touch
		chmod
		echo
		sort
		wc
		which
		pushd / popd / dirs => stack de directory
		cmp / diff

	complexe:
		grep
		sed
		tr
		awk
		xargs
		find

redirection:
	>
	2>
	&>
	here-document ("<<")
	| (pipe)

bash scripting:
	shebang
	variable
	if then else fi
	for while


à faire:
	présentation PP
	bash cheat sheet
	depot git avec exo?
